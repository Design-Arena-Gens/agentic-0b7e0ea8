import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "YouTube Automation AI ? Grow Faster with Automation",
  description:
    "Automate your YouTube workflow with AI: video SEO, auto-uploading, thumbnail generation, scheduling, and analytics.",
  keywords: [
    "YouTube automation",
    "AI for YouTube",
    "YouTube SEO",
    "thumbnail generator",
    "auto upload",
    "scheduling",
    "YouTube analytics",
  ],
  icons: [{ rel: "icon", url: "/favicon.ico" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
